import {
  mailJobReject
} from 'jobs-tuftsdaily/helpers/mail-job-reject';

module('MailJobRejectHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = mailJobReject(42);
  ok(result);
});
