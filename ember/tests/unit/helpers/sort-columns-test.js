import {
  sortColumns
} from 'jobs-tuftsdaily/helpers/sort-columns';

module('SortColumnsHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = sortColumns(42);
  ok(result);
});
