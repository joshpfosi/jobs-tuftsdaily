== jobs-tuftsdaily

This is the production branch of `jobs-tuftsdaily`. Deploying to this branch automatically tests and deploys the site to https://jobs-tuftsdaily.herokuapp.com. It can be initiated on the master branch via `rake deploy`. Eventually this functionality will be moved into CodeShip for CI.
